# Šablona pro environment soubor
# Zkopírujte tento soubor jako .env.localhost nebo .env.production a upravte hodnoty
DB_HOST=127.0.0.1
DB_NAME=sensiocz02
DB_USER=root
DB_PASS=vertrigo
DEBUG=true

# Cesta k sensio-auth instalaci (výchozí: ../sensio-auth relativně od ping)
# Na produkci s různými vhosty nastavte absolutní cestu:
# SENSIO_AUTH_PATH=/var/www/sensio-auth

